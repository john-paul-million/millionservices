server:
  port: 8081

spring:
  application:
    name: customer
  datasource:
    username: 'million'
    url: jdbc:postgresql://localhost:5432/fraud
    password: 'password'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true